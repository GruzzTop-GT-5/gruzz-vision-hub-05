# GruzzTop - Модульная архитектура для легких обновлений

## 📁 Структура проекта

```
src/
├── components/          # UI компоненты
│   ├── ui/             # Базовые UI компоненты (shadcn/ui)
│   ├── layout/         # Компоненты макета
│   └── features/       # Функциональные компоненты
├── hooks/              # Пользовательские React хуки
├── pages/              # Страницы приложения
├── services/           # API сервисы и внешние интеграции
├── types/              # TypeScript типы и интерфейсы
├── utils/              # Утилиты и вспомогательные функции
├── config/             # Конфигурация приложения
└── lib/                # Библиотеки и настройки
```

## 🔧 Конфигурация

### Основная конфигурация
- `src/config/index.ts` - Централизованная конфигурация
- Переменные окружения для разных сред
- Настройки функций и лимитов

### База данных
- Supabase как Backend-as-a-Service
- RLS политики для безопасности
- Автоматическая генерация типов

## 📝 TypeScript

### Типы данных
- `src/types/index.ts` - Все интерфейсы и типы
- Строгая типизация для всех моделей данных
- API типы для запросов и ответов

### Преимущества
- Автодополнение в IDE
- Проверка типов на этапе компиляции
- Легче рефакторинг

## 🔌 API Services

### Централизованный API слой
- `src/services/api.ts` - Все операции с БД
- Единый интерфейс для работы с данными
- Обработка ошибок и типизация

### Методы
- CRUD операции для всех сущностей
- Пагинация и фильтрация
- Загрузка файлов

## 🛠 Утилиты

### Вспомогательные функции
- `src/utils/helpers.ts` - Общие утилиты
- Форматирование данных
- Валидация
- Работа с локальным хранилищем

## 🎯 Основные принципы

### 1. Модульность
- Компоненты разделены по функциональности
- Переиспользуемые компоненты в `ui/`
- Бизнес-логика в отдельных файлах

### 2. Типобезопасность
- TypeScript для всего кода
- Интерфейсы для всех данных
- Проверка типов на этапе компиляции

### 3. Централизованное управление
- Единый API сервис
- Централизованная конфигурация
- Общие утилиты

### 4. Расширяемость
- Легко добавлять новые функции
- Модульная архитектура
- Четкое разделение ответственности

## 🔄 Добавление новых функций

### Новая сущность (например, "Orders")

1. **Типы** - добавить в `src/types/index.ts`:
```typescript
export interface Order {
  id: string;
  user_id: string;
  ad_id: string;
  status: 'pending' | 'accepted' | 'completed';
  // ...
}
```

2. **API сервис** - добавить в `src/services/api.ts`:
```typescript
async getOrders(): Promise<PaginatedResponse<Order>> { }
async createOrder(): Promise<ApiResponse<Order>> { }
// ...
```

3. **Компоненты** - создать в `src/components/`:
```typescript
// OrderCard.tsx
// OrderList.tsx
// OrderForm.tsx
```

4. **Страница** - создать в `src/pages/`:
```typescript
// Orders.tsx
```

5. **База данных** - миграция через Supabase:
```sql
CREATE TABLE orders (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  -- ...
);
```

## 🚀 Развертывание

### Экспорт в GitHub
1. Подключить GitHub аккаунт в Lovable
2. Создать репозиторий
3. Автоматическая синхронизация изменений

### Самостоятельный хостинг
- Стандартный код React/Vite
- Можно разместить где угодно
- ENV переменные управляются в хостинге

## 📚 Документация

### Для разработчиков
- Комментарии в коде
- TypeScript типы как документация
- README файлы для каждого модуля

### API документация
- Интерфейсы описывают структуру данных
- JSDoc комментарии для сложных функций
- Примеры использования в коде

## 🔧 Настройка окружения

### Development
```bash
npm install
npm run dev
```

### Production
```bash
npm run build
npm run preview
```

### Supabase
```bash
# Развертывание функций (автоматически)
# Миграции через Lovable UI
```

## 📈 Мониторинг

### Логирование
- Централизованное логирование ошибок
- Performance мониторинг
- Security логи в Supabase

### Аналитика
- Интеграция с внешними сервисами
- Отслеживание пользователей
- Метрики производительности

---

*Эта архитектура обеспечивает легкое обслуживание, быстрое добавление новых функций и простое масштабирование приложения.*